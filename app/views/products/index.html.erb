<p id="notice"><%= notice %></p> 

</p><div class="panel panel-primary">
  <div class="panel-heading"><h2>Products</h2></div>
</div>

<table class="table table-responsive">
  <thead>
    <tr>
      <th>Wine</th>
      <th>Description</th>
      <th>Label</th>
      <th>Price</th>
      <th>Action</th>
    </tr>
  </thead>
<tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= sanitize(product.description) %></td>
        <td><%= image_tag(product.image_url, class: 'list_image') %></td>
        <td><%= number_to_currency(product.price) %></td>
        <td><button type="button" class="btn btn-sm btn-info"><%= link_to 'Show', product %><br/>
            <button type="button" class="btn btn-sm btn-warning"><%= link_to 'Edit', edit_product_path(product) %><br/>
            <button type="button" class="btn btn-sm btn-danger"><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br />

<button type="button" class="btn btn-sm btn-success"><%= link_to 'New product', new_product_path %>
